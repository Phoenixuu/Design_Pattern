Interpreter
Diễn giải lệnh

Template Method
Kiểm soát trình tự, cấu trúc trong lớp cha (Template)
Cài đặt cụ thể trong lớp con 

Chain_of_responsibility
Handler lines up one by one

Command
Multiple button and each button have one function. Encapsulation them

Iterator
Dùng khi có nhiều collection mà không muốn tiết lộ cấu trúc nội bộ
Duyệt qua nhiều kiểu collection

Mediator 
Đóng vai trò trng gian giúp các đối tượng giao tiếp

Tham chiếu, tham trị, con trỏ:
Tham chiếu:: không sao chép, không tốn tài nguyên, đối tượng lớn
Tham trị: sao chép, tốn tài nguyên
Con trỏ: lưu địa chỉ, thao thác trực tiếp, có thể truyền đối tượng lớn

Obersever
thiếp lập một đối tượng (Subject) thay đổi trạng thái
Tất cả đối tượng phụ thuộc (Observers) vào nó được thông báo và cập nhật

Strategy 
Cho phép định nghĩa 1 tổ thư viện, đưa vào các class khác nhau và sử dụng thay vì kế thừa