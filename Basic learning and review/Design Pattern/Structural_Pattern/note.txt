Adapter
Sử dụng lớp hiện có
Đóng gói lại và ko xóa đi lớp ban đầu

Bridge
Liên kết chéo
1. shape
circle : shape
rectangle : shape

2. Render
Raster : render
Vector : render

3. shaperender
circlerender : shaperender
rectanglerender : shaperender


Composite
1. define interface product
2. implement product class
3. Implement composite product class
Cấu trúc phân cấp, đối tượng đơn lẻ

Decorator
Mở rộng kế thừa liên tục 

Facade 
1 lớp quản lý nhiều class phức tạp

Flyweight
FlyweightFactory manages pool flyweight
Client interact with factory instead of creating flyweights directly
giups giamr Ram vaf tanwg toocs cpu
Có nhiều đối tượng giống nhau hoặc tương tự
Cùng chia sẻ trạng thái

Proxy:
Đại diện, quản lý life cycle 
hơi phức tạp và bị chậm

